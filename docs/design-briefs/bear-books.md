# Bear Books — design brief

Paste this at the top of a Claude Design chat. **Also pick the `Bear Books` design system in Design's design-system picker** — that is where the rendered colour, type and component cards live. This file adds the product context and the hard rules, which cards cannot carry.

**What it is.** Bear Books is the studio's own double-entry bookkeeping app for macOS. Internal tooling — it has no public page and is not marketed.

**Look in one line.** The studio's own bookkeeping Mac app. Internal, never marketed.

## Type

- **Display / headings:** `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif`
  - System sans — a native macOS app, it should look native
- **Body:** `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif`
  - System sans (San Francisco / Segoe / Roboto)

## Palette

| Token | Hex | Used for |
|---|---|---|
| `ocean` | `#1A6BAD` | deep sea blue — primary |
| `aurora` | `#2EB89E` | teal-green |
| `sky` | `#5CADE6` | — |
| `sand` | `#EDDBB8` | — |
| `positive` | `#299E6B` | money in / good |
| `negative` | `#D1594D` | money out / attention |
| `warning` | `#E69E33` | — |

## Surfaces

- Page ground `#0f1720`, text `#eef4fa`, muted `#93a6bb`, hairline `rgba(140,180,215,.22)`
- Radii: cards `12px`
- Primary button: `#1A6BAD` ground, `#ffffff` label
- Secondary button: transparent, `rgba(140,180,215,.30)` border
- Shadow: `0 10px 28px rgba(0,0,0,.35)`

## Rules — not negotiable

- ⚠️ INTERNAL TOOL. Bear Books holds the studio's real financial records. It is never marketed, never screenshotted publicly, and nothing generated against this system should contain real figures.
- Positive/negative/warning are semantic — money in, money out, attention. Never use them decoratively.

---

Generated by `tools/build-design-systems.py` in the `3bears-studio-site` repo, from the real source files listed in that script's docstring. **Do not hand-edit this file** — change the source, then re-run. Every value above is one the product actually uses; none was invented.

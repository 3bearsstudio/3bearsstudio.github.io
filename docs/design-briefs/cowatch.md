# CoWatch — design brief

Paste this at the top of a Claude Design chat. **Also pick the `CoWatch` design system in Design's design-system picker** — that is where the rendered colour, type and component cards live. This file adds the product context and the hard rules, which cards cannot carry.

**What it is.** CoWatch is the shared watch-tracker built for two. Match on what to watch, follow each other's progress spoiler-safe, and never miss a new episode. Coming soon to iPhone & Android.

**Look in one line.** A shared film / TV / anime watchlist built for two people.

## Type

- **Display / headings:** `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif`
  - System sans, heavy weights — the gradient wordmark is the brand mark
- **Body:** `-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif`
  - System sans (San Francisco / Segoe / Roboto)

## Palette

| Token | Hex | Used for |
|---|---|---|
| `violet` | `#8B6CFF` | primary |
| `violet-deep` | `#6B4EF6` | — |
| `pink` | `#FF6B9D` | — |
| `pink-deep` | `#FF5C8A` | — |
| `bg` | `#0B0B11` | page ground |
| `bg2` | `#0E0E14` | — |
| `surface` | `#16161F` | — |
| `surface2` | `#1C1C27` | — |
| `line` | `#2A2A36` | — |
| `text-primary` | `#F4F4F7` | — |
| `text-secondary` | `#B4B4C2` | — |
| `text-tertiary` | `#8A8A99` | — |

## Gradients

- **Brand gradient** — `linear-gradient(120deg,#8B6CFF,#FF6B9D)`

## Surfaces

- Page ground `#0B0B11`, text `#F4F4F7`, muted `#B4B4C2`, hairline `#2A2A36`
- Radii: buttons `999px`, cards `14px`, focus ring `6px`
- Primary button: `#8B6CFF` ground, `#0B0B11` label
- Secondary button: transparent, `#2A2A36` border
- Shadow: `0 12px 32px rgba(0,0,0,.45)`

## Rules — not negotiable

- ⚠️ PROVISIONAL. CoWatch is still being reworked and its name may change. Re-run this script before trusting these tokens.
- The wordmark is the brand gradient clipped to text. Use the gradient for the mark and small accents — not as a page-wide background.
- iPhone only. It has no Android build; never claim one.

---

Generated by `tools/build-design-systems.py` in the `3bears-studio-site` repo, from the real source files listed in that script's docstring. **Do not hand-edit this file** — change the source, then re-run. Every value above is one the product actually uses; none was invented.
